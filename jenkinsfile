pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'terrform init'
            }
        }
        stage('Test') {
            steps {
               sh 'terraform plan'
            }
        }
      }
    }
